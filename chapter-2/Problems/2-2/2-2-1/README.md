In part (b) instead of using pop twice then pushing those values back again I have use stacktop. The reason is that the former approach uses 3 variables and 4 function calls and so is less efficient.
